import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export default function ProfilePage() {
  return (
    <PagePlaceholder title="프로필" description="사용자 프로필 정보가 이 자리에 표시됩니다." />
  );
}
