import { PagePlaceholder } from '@/components/common/PagePlaceholder';

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="로그인"
      description="Authentication Feature 구현 시 로그인 폼이 이 자리에 표시됩니다."
    />
  );
}
