# Cloud Functions 전용 환경 변수 (프론트엔드에는 절대 노출하지 않는다)
# 로컬 에뮬레이터 실행 시 functions/.env.local 로 복사하여 사용한다.
# 배포 시에는 Firebase Secret Manager를 사용한다: firebase functions:secrets:set OPENAI_API_KEY
OPENAI_API_KEY=
